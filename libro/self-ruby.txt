#Self

En cada punto que su programa se ejecuta, hay un solo y solo uno **self** - el objeto actual o por omisión accesible por tí en tu programa. Puedes decir que objeto **self** representa siguiendo un pequeño conjunto de reglas.

h2. Contexto del nivel superior

El contexto del nivel superior es antes de que entres a otro contexto, tal como la definición de una clase. Por lo tanto el térmito nivel superior se refiere al código escrito fuera de una clase o módulo. Si abres un archivo de texto y escribes:

Métodos singleton - son los relacionados a un objeto particular que puede ser llamado por un solo objeto. Cuando se ejecuta un método singleton, **self** es el objeto que es propietario del método, como se muestra debajo:

**self** dentro de método singleton (un método de clase, en este caso) es el objeto del método singleton que él es.



